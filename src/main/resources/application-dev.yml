spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipe_app_db_dev
    username: mrw_dev_user
    password: pass
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#          javax:
#            persistence:
#              schema-generation:
#                create-source: metadata
#                scripts:
#                  action: create
#                  create-target: recipe_app_database_create.sql